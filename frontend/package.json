{
  "name": "pos-frontend-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=admin",
    "dev:admin": "npm run dev --workspace=admin",
    "dev:caja": "npm run dev --workspace=caja"
  }
}
